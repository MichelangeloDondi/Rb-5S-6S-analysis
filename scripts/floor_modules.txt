# THE FORTY-SECOND FLOOR'S MODULES, in ONE place because two readers must never disagree about them.
#
# `scripts/prefloor.sh` runs these before a board is summoned and again before a commit, repeating until
# green, so by the time the gate starts they are green BY CONSTRUCTION. The gate therefore skips them and
# grades everything else: that is the REDUCED GATE (owner, 2026-09-19), and the minutes it saves are
# minutes the machine spends on science instead of on a verdict already in hand.
#
# THE SPLIT IS ONLY LEGAL WITH ITS PLANT. This record's own rule is that a gate grading a different
# population is worse than a slow gate, so `gate_split.py --verify` collects the floor's nodes and the
# reduced gate's nodes and refuses unless their union is the whole suite and their intersection is empty.
# Removing a line here moves a module INTO the gate, which is safe; adding one moves a module OUT of the
# gate and into a set that must actually be run before every board and every commit, which is the only
# direction that can lose coverage.
#
# THE ORDER MATTERS SINCE 2026-09-25. The floor runs these with --dist worksteal, which hands each worker a
# contiguous share of the tests in THIS order before any stealing, so the module holding the floor's longest
# single test -- test_docs_canonical's whole-suite collection for the advertised counts, 11.5 s of a 14.8 s
# stage measured alone -- is listed first and that test starts at once instead of behind twelve modules. It
# joined the floor with the document guards under "RED AT THE GATE ON A GREEN FLOOR" below.
tests/test_docs_canonical.py
tests/test_repo_hygiene.py
tests/test_prose_style_ratchet.py
tests/test_references.py
tests/test_reference_coverage.py
tests/test_history_form.py
tests/test_agonistic_ratchet.py
tests/test_docs_math_render.py
tests/test_docs_links.py
tests/test_docstring_counts.py
# tests/test_tqm_report.py -- MOVED TO THE GATE 2026-09-24 on the owner's ruling
# that the floor must finish under 60 s. Measured that day: its single cell
# test_the_report_runs_end_to_end took 163.19 s of a 165.94 s stage, 98.3 per cent,
# while the other ten modules together took about 2.7 s. It is not a prose or doc
# guard, which is what this list is for; it is an end-to-end report run, and the
# gate is where an expensive question about a tree is answered. THIS FILE IS THE
# ONE LIST prefloor.sh and gate_split.py both read, so commenting it out here
# moves it INTO the reduced gate by construction and loses no coverage --
# gate_split --verify proves the union is still the whole suite.
tests/test_lit_consistency.py
# RED AT THE GATE ON A GREEN FLOOR (the TQM process check of 2026-09-24): the floor read
# green and the gate then failed 52 nodes, 19 of them in these four document guards, which read text and committed
# cells only and take 7.4 s together serially on the sandbox copy, 6.3 s of it the test-count collection. The
# check's pre-decided consequence is to repair the map, so they move from the gate into the floor.
tests/test_navigation_footer_is_last.py
tests/test_navigation_chain_is_coherent.py
tests/test_results_index_is_complete.py
# THE RAIL AT THE GATE (2026-09-24, the kaizen of commit 235b54ac): the landing's first attempt returned red on
# the 1500-word rail of a page with no header block, which only this module grades, and it reached the gate because
# the floor did not run it. It reads text only and takes 0.56 s, so it moves into the floor.
tests/test_prose_shape.py
# V7.1's board (2026-09-26): a module renamed away leaves no route to its old name; five survived O61 stage A outside
# the dotted-routes test's population (a bare route on a methods page, every .py docstring). Text only, about a second.
tests/test_retired_module_routes.py
# O58, 2026-09-25: the model-terms registry, its start-refusing preflight at every twin and Monte Carlo entry (planted
# both ways, the two ratchets included) and the wiki status guard. Owner order O58 names the floor as where these are
# summed; the module reads the registry and its renderings and takes about 2 s.
tests/test_model_registry.py
