# The working notes

A large share of the files here are preregistrations. A preregistration is a
specification written and committed before the run whose outcome it scores. The
estimator, the thresholds, the trace census and the stop conditions are fixed in
the record while the answer is still unknown, so the run can only confirm them
or fail them. That ordering is what makes the outcome interpretable, because a
threshold chosen after seeing a fit scores nothing. The outcomes themselves are
reported in [PREREGISTRATION_RESULTS.md](../PREREGISTRATION_RESULTS.md), which
is where a prediction is marked against what happened and where the dated
addenda live, including the ones that record a failure. The remaining files
were written after the fact rather than before: design studies of
measurements that do not yet exist, records that close questions the
analysis had left open, and open notes on questions still standing.

| file | what it specifies or records | kind |
|---|---|---|
| [beta_self_pooling_prereg.md](beta_self_pooling_prereg.md) | One shared self-broadening slope across the four 993 nm hyperfine lines in place of four independent fits: the physics that licenses the pooling, the pooled estimator and its variance, the scope, and the predictions the implementation then had to meet. | preregistration |
| [full_dataset_fit_prereg.md](full_dataset_fit_prereg.md) | The joint fit over every canonical trace, with the collisional width held under a prior and one profiled light-shift coefficient: trace census, parameter hierarchy, priors, load-time quality checks, the coefficient grid, the profile families and seeding order, and the acceptance criteria. | preregistration |
| [m28_reproducibility_prereg.md](m28_reproducibility_prereg.md) | A re-run of that same fit on bit-identical inputs, to separate a genuine response to recalibrated inputs from optimizer chain variation. The reproduction threshold, 3 per cent, is fixed before the run. | preregistration |
| [ruler_validity_and_trim_prereg.md](ruler_validity_and_trim_prereg.md) | The frequency ruler's tooth-labelling rule, the re-index ladder and its acceptance ceiling, the residual-tail trimmer and its null calibration, the outlier rule, and the eligibility conditions for two figures. Eight dated amendments follow the original text, and the opening table gives the current state of every rule. | preregistration |
| [companion_inclusive_refit_prereg.md](companion_inclusive_refit_prereg.md) | What the fits would do with atomic saturation, hyperfine pumping and transit depletion inside the forward model rather than stated as a looseness around it. The construction that could separate the pumping term, which is the only one of the three that differs between the four hyperfine lines, and a power calculation predicting in advance that this dataset misses its own computed companion by a factor of twenty to forty. Five numbered predictions and four stop conditions. | preregistration |
| [s0_block_bootstrap_prereg.md](s0_block_bootstrap_prereg.md) | A block bootstrap of the power-lever light-shift limit, stratified by peak, as the sharper alternative to carrying block-to-block over-dispersion as one global factor. The resample count and the seed are frozen with the script that implements them. | preregistration |
| [model_selection_prereg.md](model_selection_prereg.md) | What switching the model-complexity criterion from BIC to AIC would re-decide, fixed before any recomputation: the penalty arithmetic showing n is not one number in this project, the comparison sites each carrying a prediction, the criteria panel that is always reported as numbers, and the rule that no flip enters the record without the owner. Worked back from the committed CSVs, the sigma_laser sharing comparison is the one site the switch demonstrably flips. | preregistration |
| [stark_coverage_prereg.md](stark_coverage_prereg.md) | M14. Whether the AC-Stark profile bound, quoted at 95 per cent, actually covers 95 per cent. The fit rails at the parameter boundary, where a likelihood ratio is not chi-square with one degree of freedom, so the threshold may be conservative or not and only simulation separates them. |
| [stark_coverage_postscript.md](stark_coverage_postscript.md) | What M14 returned. The construction OVER-covers everywhere the published bounds live, and the archive's own bound sits at the zeroth percentile of its simulated distribution, which says more about the noise arm than about the archive. |
| [band_excess_is_model_form.md](band_excess_is_model_form.md) | The residual excess outside the fit window acquires a candidate mechanism, and it is the LINESHAPE rather than the atom. One joint fit over every canonical trace, each keeping its own free polynomial baseline, leaves it standing at 3.6 sigma under cubic freedom, and it tracks the model's own in-band profile height at 8.65 sigma while vapour density is a null predictor. |
| [campaign_only_stark_profile.md](campaign_only_stark_profile.md) | The campaign-only Stark refit's construction and profile, published so the chapter on joint-fit legitimacy claims nothing the public evidence does not show. Diagnostic, not the record: three passes agreeing at the third decimal, the wing variant beside it, and the statement of what neither column establishes. |
| [guided_mode_two_photon_design.md](guided_mode_two_photon_design.md) | What changes if the same two-photon measurement runs in a hollow-core fibre instead of a vapour cell: how the intensity is delivered, trap shifts and vibrational structure, the signal and background budgets, a readout comparison, fibre feasibility, and what would have to be measured before any of it is a plan. Nothing in it is scheduled, agreed or costed. | design note |
| [transit_width_resolved.md](transit_width_resolved.md) | How the transit-width tension was settled: one missing crossing-flux factor in the transit Monte Carlo, the closed-form validation of the fix, and the beam-measured waist moving from 32 to about 50 µm and then to the measured 64 µm. An earlier reading of the same evidence is retracted in place and labelled. | record |
| [centre_channel_cannot_be_revived.md](centre_channel_cannot_be_revived.md) | Why the line-centre AC-Stark lever cannot be recovered from this dataset: a calibrated forecast of what each session could deliver, then the obstruction that spends it in each one, and the single measurement that would reopen the channel. | record |
| [eom_traces_with_the_rf_off_set.md](eom_traces_with_the_rf_off_set.md) | What the modulated traces add when fitted with the unmodulated set: the census that corrects a docstring, the bracket powers measured from comb area, and the sign argument that removes saturation from the list of explanations for the modulated cores being wider. | open |
| [amplitude_departure_from_p2.md](amplitude_departure_from_p2.md) | The two-photon amplitude departs from the square-of-power law: three of the four campaign lines exclude 2 under the block bootstrap, the rehearsal's alternating-direction ladders replicate the departure and rule out the order-dependent causes, and the exponent tracks brightness rather than hyperfine branching, so the signature belongs to the detection chain and not to the transition. The mechanism is unattributed, nothing in results/ moves, and a measured detector response curve is what would settle it. | open |
| [running_wave_and_waist_design.md](running_wave_and_waist_design.md) | Two geometry choices for a future fixed-lock session: a frequency-shifted retro arm so the fringes run instead of standing, and the beam waist. Each is reported with the first pass that was wrong beside the answer, because the running wave needs a shift that outruns the thermal velocity spread rather than the linewidth, and the waist choice is decided by a saturation term that changes the planned session's predicted skew by a factor of three. | design |
| [widescan_block_design.md](widescan_block_design.md) | The wide-scan acquisition block, sized so the next session can run it cold: a 2400 MHz span set by a background-degeneracy calculation rather than by where the pedestal looks visible, the record length that dissolves the span-against-shape tradeoff, piezo shape and dwell, the forward-modelled detection significance with its correlated-noise correction, six go/no-go checks for the day, and the two quantities no scan setting can separate. | design |
| [two_photon_saturation_companion.md](two_photon_saturation_companion.md) | The two-photon Rabi frequency at the campaign's brightest field, computed because a question about a third photon needed it, and the consequence that atomic saturation broadens the line several times harder than the AC-Stark ramp the width-channel light-shift lever is built on, with the same power signature and absent from that forward model. The direction of the bias is favourable, so no committed bound changes, and the note states what would settle it. A dated addition carries a second companion of the same signature, hyperfine pumping by the real cascade, whose width is exactly the branching fraction times the saturation width, and a second addition records the joint three-session bound re-profiled with the companion in the model, which reproduces the committed bound unpatched and then tightens it by a factor 2.21. | open |
| [onf_candidate.md](onf_candidate.md) | The nanofiber candidate, sized: the cold trap-off mode as an independent laser-width instrument, the atom-surface tail as a C3 measurement for the 6S state, hot vapor as a transit-kernel test, and the Stark geometry seam exercised for free. Every number is produced into onf_candidate.csv with a per-row basis label, and the lab facts the estimates wait on are listed for replacement. | design study |
| [extended_lever_and_skew_projection.md](extended_lever_and_skew_projection.md) | Two projections for the quantities dossiers, each with its construction: the extended temperature lever, where a 170 C grid would put the median collisional bound below the van der Waals anchor under assumptions the note labels optimistic, and the 16 micron skew channel, whose achievable precision is deliberately not projected because the perturbative cost scaling does not extrapolate to a shift comparable to the linewidth. | design study |
| [vdw_difference_potential_and_4d_channel.md](vdw_difference_potential_and_4d_channel.md) | Which van der Waals coefficient enters the self-broadening anchor, the difference rather than the upper pair coefficient, with the derivation in the source, the numbers that moved, and the sites still carrying the earlier value. Its last section records the 6S to 4D interval as a candidate inelastic channel that nothing in the pipeline depends on. | record |

The notes are append-only. A correction enters as a dated addition, an
amendment or a postscript placed after the original text, and the passage that
stated the prediction is left exactly as it was written. Where an amendment
changes a rule, the amendment governs and the replaced wording stays visible,
so what was fixed in advance of the data can be told apart from what was fixed
after it. Editing a prediction into agreement with its outcome would leave a
document that reads as though it had always been right.
