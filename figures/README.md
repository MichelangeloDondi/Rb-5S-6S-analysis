# The figures

Every PNG in this directory is generated, and each one is rebuilt from the
repository rather than kept as a saved plot from an interactive session. Most
are drawn by [`scripts/make_figures.py`](../scripts/make_figures.py), which
reads the committed CSVs under [`results/`](../results/README.md) and computes
every number it prints. Two figures have their own producers.
`fig0_spectrum.png` comes from
[`scripts/make_fig0_spectrum.py`](../scripts/make_fig0_spectrum.py), which
reloads one condition's repeats from the raw archive and re-runs the joint fit
end to end. `fig9_polarizability_ladder.png` comes from
[`scripts/run_polarizability_ladder.py`](../scripts/run_polarizability_ladder.py),
which sums over states in `rb5s6s/polarizability.py` and writes nothing to
`results/`. The four fit galleries (fig16, fig18, fig21 and fig22) take their
shared parameters from `results/global_archive_fit.csv` and the traces
themselves from [`data_raw/`](../data_raw/README.md), refitting only each
trace's own amplitude, centre, background and detector saturation.

Two of those paths need the traces themselves: `make_fig0_spectrum.py` and the
fig16, fig18, fig21 and fig22 galleries inside `make_figures.py`. They run in a
copy of the repository that carries `data_raw/`'s traces and not in one that
carries only the manifest, where the committed PNGs are the record instead.
[`data_raw/README.md`](../data_raw/README.md) says which copy this is.

To redraw them:

```
python scripts/make_fig0_spectrum.py
python scripts/make_figures.py
python scripts/run_polarizability_ladder.py
```

The first two lines are the figure steps of
[`scripts/run_all.sh`](../scripts/run_all.sh), so a full pipeline run already
produces them. The third is run on its own.

Each figure that `make_figures.py` writes carries a fingerprint of the
results CSVs in its PNG metadata, stamped at save time.
[`tests/test_figures_fresh.py`](../tests/test_figures_fresh.py) reads that
stamp back for the data-driven figures and fails when a committed PNG was
drawn from results that have since moved, which makes the check independent
of the matplotlib version that drew the figure. The two figures with their own
producers carry no stamp, and `fig0_spectrum.png` is exempt by design because
it is drawn from the frozen raw archive. Every figure also carries a footer
line naming the sources it is drawn from and the command that regenerates it.

Ten figures are cited by no document. Their rows below name the claim they
support instead of a citing passage.

| file | what it shows | drawn by | discussed in |
|---|---|---|---|
| `fig0_spectrum.png` | one fitted spectrum, the 993.4192 nm line at 130 °C and 225 mW, with the composite model overlaid and a residual panel | `make_fig0_spectrum.py` | [README.md](../README.md) |
| `fig1_width_vs_density.png` | total line width at half maximum against vapour density, one series per hyperfine component, with cell temperature on the top axis | `make_figures.py` | [methods/07_what_we_found.md](../docs/methods/07_what_we_found.md) |
| `fig2_power_sweep.png` | width and amplitude against drive power, the amplitude following the two-photon square law and the width carrying no significant slope | `make_figures.py` | [methods/07_what_we_found.md](../docs/methods/07_what_we_found.md) |
| `fig3_transit_mc.png` | the Monte Carlo transit contribution against beam waist, with the waist at which it would account for the observed total | `make_figures.py` | [README.md](../README.md), [PLAN.md](../docs/PLAN.md) |
| `fig4_amplitude_ratios.png` | within-isotope area ratios against the parameter-free degeneracy law, each prediction marked on its own dashed line. For a scalar two-photon operator the transition strengths scale as the upper-level statistical weight (2F+1), and the two measured ratios are drawn against those two predictions. The bars combine the scatter over repeats with the drift between measurement blocks, which is far larger than the difference the prediction would show, so this dataset cannot test the law | `make_figures.py` | [methods/07_what_we_found.md](../docs/methods/07_what_we_found.md) §5.7, which carries the reading of the figure |
| `fig5_pooled_width.png` | the four components pooled into one width budget against density, with the shared laser width against temperature beside it | `make_figures.py` | cited nowhere. Supports the self-broadening bound, [RESULTS.md](../docs/RESULTS.md) C1, by showing the pooled trend the individual components are too noisy to carry |
| `fig6_gamma_floor.png` | the fitted collisional width rising about 1.5-fold while the density rises 52-fold, which is why the coefficient is read as a floor | `make_figures.py` | [README.md](../README.md), [PLAN.md](../docs/PLAN.md) |
| `fig7_identifiability_profile.png` | the profile-likelihood map of the collisional-width and laser-width degeneracy, the wide topology and a zoom against the local covariance ellipse. The two widths are not separately identifiable at this condition: the likelihood has a long valley along which they trade, so the split is set by the fit model rather than by the data. The condition is 993.4192 nm, cell at 130 °C, 225 mW, and the transit width and every remaining fitted parameter are re-minimised at each point of the grid. The white curves are the joint 68 and 95 percent contours, and beyond the grey dotted line the fitted transit width has reached zero and is held there | `make_figures.py` | [methods/06_the_statistics.md](../docs/methods/06_the_statistics.md). Neither citing document embeds it, so this row is its caption |
| `fig8_ruler.png` | one EOM ruler trace with its constrained seven-tooth comb fit, and the free-centres nonlinearity map beside it | `make_figures.py` | [methods/05_the_frequency_ruler.md](../docs/methods/05_the_frequency_ruler.md) |
| `fig9_polarizability_ladder.png` | the polarizability difference against wavelength for the 5S to 6S and 5S to 7S ladders, each drive marked and each crossing labelled | `run_polarizability_ladder.py` | [BIG_PICTURE.md](../docs/BIG_PICTURE.md) |
| `fig10_degeneracy_vs_observable.png` | the twenty power-sweep conditions as error ellipses over contours of constant total width, and the same conditions' total width against power | `make_figures.py` | [BIG_PICTURE.md](../docs/BIG_PICTURE.md), [RESEARCH_DECISIONS.md](../docs/RESEARCH_DECISIONS.md) |
| `fig11_laser_history.png` | the campaign's laser frequency within each run of unchanged scope-window setting, with a visible break at every setting change, the one long untouched stretch, and the within-epoch step distribution. No wavemeter log survives, so this is what the traces themselves can and cannot say. Panel (a) draws 296 traces in 60 segments of unchanged oscilloscope window setting, each segment referenced to itself, because across a change of that setting the offset is unknown and the record breaks. Panel (b) is the quietest well-sampled segment, 993.4154 nm, 10 traces over 3.4 min held to 0.17 MHz peak to peak. Panel (c) has half the steps below 0.21 MHz and a separate group running out to 15.5 MHz. The record does not say what produced those, though a re-lock of the reference cavity would look like this | `make_figures.py` | cited from [DATA.md](../docs/DATA.md), which now shows it. Supports the drift limitation in [DATA.md](../docs/DATA.md): an offset means nothing across a setting change, and within one setting the excursion is about 1 MHz. This row is the figure's caption |
| `fig12_ramp_construction.png` | how the intensity profile of a focused Gaussian beam becomes a triangular light-shift distribution, in four steps, with no data and no fitted parameters | `make_figures.py` | [THEORY_NOTE.md](../docs/THEORY_NOTE.md), [PLAN.md](../docs/PLAN.md) |
| `fig13_level_scheme.png` | which levels are driven and which arm is detected, in the term-diagram idiom, with the digitised cavity scan in side panels | `make_figures.py` | [methods/01_the_measurement.md](../docs/methods/01_the_measurement.md), [APPARATUS.md](../docs/APPARATUS.md) |
| `fig14_wavemeter_reconstruction.png` | the 2025-06-11 wavemeter record, the sawtooth model fitted to it, and the resulting floor on unmodelled laser motion | `make_figures.py` | [APPARATUS.md](../docs/APPARATUS.md), [PREREGISTRATION_RESULTS.md](../docs/PREREGISTRATION_RESULTS.md) |
| `fig15_drift_story.png` | the drift problem photographed, the campaign record reconstructed from its own traces, and what each drift regime licenses | `make_figures.py` | [README.md](../README.md), [PLAN.md](../docs/PLAN.md) |
| `fig16_fit_gallery.png` | the brightest campaign trace of each component against the committed shared model, with residuals, four panels in one frame | `make_figures.py` | [README.md](../README.md) |
| `fig17_magic_wavelengths.png` | the 5S and 6S polarizabilities and their difference, with the three zero crossings and each crossing's Monte Carlo band | `make_figures.py` | [README.md](../README.md) |
| `fig18_single_4121.png` | one full-page panel for the 993.4121 nm component: the same trace and shared model as fig16, with a parameter box naming every number as shared or per-trace. The model is a Lorentzian core, natural plus collisional, convolved with a Gaussian envelope set by the laser line width and the transit time, with the detector's saturation included. The shared block holds at the widths common to the whole campaign, and only this trace's amplitude, centre and background are refit. Reading the box: a shared laser width is shared by every trace taken at this temperature in one session and the shared fit publishes no separate uncertainty for it, the same goes for the shared collisional width, the transit width is held fixed at the value the assumed geometry implies because the beam waist has not been measured, the fitted centre is on this trace's own scan axis whose origin is arbitrary, and where the reduced chi-squared falls below one the per-point noise assumed in the fit is conservative so the uncertainties beneath it are upper bounds | `make_figures.py` | deliberately not embedded. It is a sibling of `fig18_single_4192.png`, which [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md) shows for one peak. Showing all four in the chapter would repeat the same statement four times, and a reader checking that the shape holds on the other peaks wants the files rather than the prose. Supports the composite lineshape model of [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md), whose shared and per-trace split the parameter box states. This row is the caption for all four panels |
| `fig18_single_4154.png` | the same panel for the 993.4154 nm component | `make_figures.py` | deliberately not embedded. It is a sibling of `fig18_single_4192.png`, which [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md) shows for one peak. Showing all four in the chapter would repeat the same statement four times, and a reader checking that the shape holds on the other peaks wants the files rather than the prose. Supports [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md), as above |
| `fig18_single_4192.png` | the same panel for the 993.4192 nm component | `make_figures.py` | cited from [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md), which now shows it. Supports [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md), as above |
| `fig18_single_4207.png` | the same panel for the 993.4207 nm component | `make_figures.py` | deliberately not embedded. It is a sibling of `fig18_single_4192.png`, which [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md) shows for one peak. Showing all four in the chapter would repeat the same statement four times, and a reader checking that the shape holds on the other peaks wants the files rather than the prose. Supports [methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md), as above |
| `fig19_width_trends.png` | the two broadening laws side by side: raw contiguous width against density for all four components, drawn as the headline self-broadening estimator itself fits it, with the pilot point outside the fit, and beside it the same widths against drive power under the thin wedge of growth the light-shift bound still permits. Collisions add width in proportion to density, W = floor + beta N, read here as one slope shared across the four lines with a floor per line, because the hyperfine components share one collision physics. That shared slope is smaller than twice its own uncertainty, so it is a bound and not a measurement. Left panel, the error bars are the repeat and between-block scatter that feeds the quoted 95 percent bound and are not the bound itself, which adds a one-sided Student-t inflation on one degree of freedom. The 70 °C points sit about 6 percent low on three of the four lines, a documented narrowing of weak traces, and because it narrows the anchor it steepens the fitted slope, so it can only leave the bound conservative. The shaded band on each line is the shared slope's uncertainty pivoted on that line's lowest-density point, so it opens with density. Two candidate sources are weighed and refused, both under section 8 of [the ruler specification](../docs/notes/ruler_validity_and_trim_prereg.md) and both the owner's to overrule: the rehearsal session, whose scan rate comes out of a fit rather than a measurement, and the calibration-comb traces, whose widths would need a sideband amplitude model. The fitted comb heights of all 104 calibration traces are archived so such a model can be tested. The pilot point's horizontal bar is the density range left open because the oven label gives only 70 to 90 °C. Right panel, the width channel cannot resolve the light shift: the light-shift broadening coefficient is below 2.81 MHz per watt at 95 percent confidence from the profile likelihood, each dashed curve is that component's lowest-power width plus the largest light-shift broadening the bound still allows, and the shaded wedge between them is every rise the bound leaves open. The measured widths move by about eight times the wedge's height, which is scatter between measurement blocks, and that scatter exceeds the within-block uncertainty by a factor 3.7 in variance. The laser line width is left out of the wedge, because at a fixed condition it cannot be separated from the collisional width | `make_figures.py` | cited from [RESULTS.md](../docs/RESULTS.md) C1, which now shows it. Supports the self-broadening bound, [RESULTS.md](../docs/RESULTS.md) C1, and is the only figure that draws that estimator's own construction. This row is its caption |
| `fig20_method_loop.png` | a schematic of the method: an observation, an identifiability test, then either a claim or a named limitation and the measurement that lifts it, with two worked examples from this archive beneath. Most of what this work reports today sits on the lower branch, where two mechanisms cannot yet be separated, and the two cases drawn beneath are the important ones | `make_figures.py` | cited from [BIG_PICTURE.md](../docs/BIG_PICTURE.md) sec 1.3, which now shows it. Supports the framing of [BIG_PICTURE.md](../docs/BIG_PICTURE.md), that the bounds and the limitations they name are the result. This row is its caption |
| `fig23_hyperfine_pumping.png` | why a real decay ends an atom's participation in the line, and what that costs. Panel (a) is the term diagram with the branch drawn: the 5P decay does not preserve F, so an atom that decays mid-crossing can land in the other ground state, which this archive resolves as two of its own four lines, hundreds of linewidths away. Panel (b) is how often that happens per crossing, on the axis and averaged over the signal. Panel (c) puts the two width companions beside the AC-Stark ramp they are confused with, at the campaign maximum, and the ramp is the smallest of the three. A bar chart rather than a power sweep because all three grow as the square of the power, which is the whole difficulty, and because the ramp increment falls under the frequency grid's own resolution at the low end | `make_figures.py` | [docs/methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md) and [notes/two_photon_saturation_companion.md](../docs/notes/two_photon_saturation_companion.md) |
| `fig24_weak_field_limit.png` | where the ramp law's own assumption stops holding. The construction rests on the signal going as the square of the intensity, which is the leading term of the excited fraction and not the fraction itself. Panel (a) draws the weight against the approximation, with the archive and the proposed tight-focus session marked: the saturation parameter carries the two-photon Rabi frequency squared, so it grows as the fourth power of the inverse waist while the shift grows only as the second, and the limit is left long before the shift becomes large. Panel (b) is the consequence, the predicted ramp skewness against waist computed both ways. The sign flip survives and the magnitude does not | `make_figures.py` | [docs/THEORY_NOTE.md](../docs/THEORY_NOTE.md) sec 2.0a, [docs/CLAIMS.md](../docs/CLAIMS.md) and [notes/running_wave_and_waist_design.md](../docs/notes/running_wave_and_waist_design.md) |
| `fig25_retro_combination.png` | why the shift and the two-photon rate take different combinations of the same two beam arms. The atom sits in a standing wave of contrast set by the retro return fraction. The AC-Stark shift is linear in intensity, so it follows the fringe mean, the arithmetic combination. A Doppler-free two-photon amplitude is linear in the square of the field and only its wavevector-cancelling term is Doppler-free, so it takes the fringe amplitude, the geometric combination. Panel (b) is the gap between them against the return fraction, with this bench and a poorer retro marked: the correction is invisible here and is not invisible generally, which is why the formula carries it. The asymmetry is the useful part, since the rate has no position dependence and the shift does | `make_figures.py` | [docs/methods/03_the_ac_stark_ramp.md](../docs/methods/03_the_ac_stark_ramp.md) and [notes/two_photon_saturation_companion.md](../docs/notes/two_photon_saturation_companion.md) |
| `fig26_lineshape_kernels.png` | what the observed line is made of. Panel (a) draws the four kernels of the composite model at the campaign's representative widths: the natural and collisional Lorentzians, the laser Gaussian and the two-sided exponential of transit. They differ in shape as well as width, and the transit kernel's central cusp is what lets a fit tell it from a Gaussian of the same width. Panel (b) assembles the line one convolution at a time, each step labelled with the width it reaches. The natural width is about two thirds of the observed total and everything above it is apparatus. The first two steps are one step in the code, because two Lorentzians of any widths convolve to a Lorentzian, which is also why the collisional width is separated by its density dependence rather than by shape | `make_figures.py` | [docs/methods/02_the_lineshape.md](../docs/methods/02_the_lineshape.md) |
| `fig27_radiation_environment.png` | the three radiation fields a hot cell puts on these atoms, on one pair of axes. Panel (a) is the thermal occupation number against wavelength at 130 °C, with every line of the cascade marked on it and the band where the thermal photons actually are shaded: the detected 795 nm line sits twenty decades below that band and the infrared cascade legs twelve, which is the whole reason blackbody light does nothing here. Panel (b) is the consequence, every channel scored against the two-photon drive that makes the signal, over nineteen decades of rate. The one thermal channel that clears the floor is 6S to 6P at 2.7 µm, still two parts per million of the natural decay, and the only error bar in the panel is the trapped-infrared row's, because its standoff is geometric where everything else is atomic | `make_figures.py` | [docs/methods/04_the_composite_model.md](../docs/methods/04_the_composite_model.md) |
| `fig28_cascade_resolved.png` | why the pumping branching is not the degeneracy weight, and why the correction is nevertheless two numbers. Panel (a) resolves one line's cascade by intermediate F. The level that is fed with 0.17 of that leg and loses none of it, because a J = 1 photon cannot change F by two, so it returns the atom to the level it came from. Panel (b) is the point. Small markers are the individual intermediate levels, which scatter from 0 to 0.67, and large markers are the leg sums, which sit on exactly 8/9 and 4/9 for all four lines and both isotopes. That sum rule is derived as 2(1-p) with p the purely electronic non-flip probability, and checked again as an exact-rational density matrix with every coherence kept | `make_figures.py` | [docs/THEORY_NOTE.md](../docs/THEORY_NOTE.md) |
| `fig29_isotope_transit.png` | why the two isotopes do not share a transit width, and why the fits share one anyway. Panel (a) is the kernel against temperature for both masses, 12 kHz apart at 140 °C on a width quoted to 0.01 MHz. Panel (b) is the reason that is stated rather than corrected: against density, which is the lever the collisional coefficient is read from, the gap is almost all constant offset, and the free per-line core width absorbs a constant. The dashed line is what one standard error on the measured difference between the isotopes would look like on the same axes, and it is a factor of 240 steeper | `make_figures.py` | [docs/methods/02_the_lineshape.md](../docs/methods/02_the_lineshape.md) |
| `fig21_joint_fit_five.png` | five repeats of one condition under a single shared line shape, the shared widths in the header, and each row printing its own fitted centre, peak height and reduced chi-squared. Four parameters are refitted for each repeat, because the frequency lock drifts between them: the line centre, the peak height, the background level and the detector saturation. The points are the measured signal and the line is the shared model. The point uncertainties come from the baseline scatter, which is conservative in the line core, so the reduced chi-squared sits below one. Each residual strip divides by that point's own uncertainty | `make_figures.py` | cited from [notes/full_archive_fit_prereg.md](../docs/notes/full_archive_fit_prereg.md), which now shows it. Supports the shared-shape archive fit preregistered in [notes/full_archive_fit_prereg.md](../docs/notes/full_archive_fit_prereg.md). This row is its caption |
| `fig22_joint_fit_twenty.png` | the same shared line shape across all twenty campaign power-sweep conditions, brightest repeat each, nothing retuned per panel. The model is a Lorentzian core, natural 3.49 MHz plus collisions, convolved with the laser width and the transit kernel, with detector saturation, and the shared collisional, laser and transit widths are in `results/global_archive_fit.csv`, which the figure's own footer cites. Refitted for each trace, because the lock and the detector drift: line centre, peak height, background level, detector saturation. The line shape itself is identical in all twenty panels. Every panel is scaled to its own trace, so peak heights cannot be compared across the grid and the numbers on the left apply to the first column only. The residual strips are in units of that trace's own point-by-point uncertainty, estimated from the baseline scatter, which overestimates the noise where the signal is strong, so the reduced chi-squared falls below one and the widths quoted above are correspondingly conservative. The signal is weakest in the 25 mW column, the lowest drive power of the sweep | `make_figures.py` | cited from [notes/full_archive_fit_prereg.md](../docs/notes/full_archive_fit_prereg.md), which now shows it. Supports the same fit as fig21, [notes/full_archive_fit_prereg.md](../docs/notes/full_archive_fit_prereg.md). This row is its caption |

The colour of a component is fixed across every panel here, so a colour means
the same hyperfine line wherever it appears.

An axis label on these figures is a quantity and a unit, nothing more. Every
caveat, status word and reading that used to be printed inside a canvas now
lives in the caption of the document that references the figure, or in the row
above where no document references it. Two of those caveats travel with every
absolute width and are worth restating here. The beam waist has not been
measured, the knife-edge scan is pending, and absolute widths ride on it, so a
smaller waist would lower a whole series together. The Rb density axis is
logarithmic and carries a 20 per cent scale systematic from the vapour-pressure
model, common to every point on it. Where a residual strip is drawn, it divides
each residual by that point's own uncertainty. Frequency axes name their own
convention: a "two-photon detuning" is on the transition axis, a "laser
detuning" is on the per-photon axis, and the transition axis is twice the laser
axis ([CLAIMS.md](../docs/CLAIMS.md) states the convention). The provenance footer under each
figure is a separate contract, named in
[`tests/test_figure_register.py`](../tests/test_figure_register.py), and it
keeps its file paths and pipeline vocabulary.
