# The sdist carries the advertised entry points: the five-minute example
# and the per-module map, both named by START_HERE.md and docs/ADAPTING.md
# and both absent from the 4.0 through 4.3 distributions (the 2026-08-17
# audit's gaps one and two, confirmed live against the 4.2-era wheel).
include rb5s6s/README.md
recursive-include examples *.py
